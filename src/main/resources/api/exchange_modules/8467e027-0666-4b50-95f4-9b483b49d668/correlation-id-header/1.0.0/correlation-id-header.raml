#%RAML 1.0 Trait
  usage: apply this trait to enable api consumers to send their own correlation id for tracking end to end transactions. mule will use its own transaction-id header but consumer can correlate their own correlation id to mule transaction-id
  headers:
    x-correlation-id:
      type: string
      required: false
      description: correlation id to maintain over apis